# RFC-5E536A146A81 Red 재현 보고서

기준 SHA는 `383ceca93a602abd8d7e96d5e39b4139b0708d24`이고 실행 브랜치는 `wiki-auto/run-20260715005500z-7e9858`이다.

## 고정한 실패

- 다른 행위자가 같은 `Asia/Seoul` 날짜에 만든 캠페인이 있어도 두 번째 캠페인이 생성돼 포트폴리오 전역 하루 상한을 위반했다.
- 관심사에서 생성된 캠페인에 `project_id`가 없어 상위 프로젝트 분류가 손실됐다.
- `project_id`가 없거나 등록되지 않은 관심사를 생성기가 거부하지 않았다.
- `wiki/projects/` 프로젝트별 탐색 뷰 생성기가 존재하지 않아 캠페인·실행·공유 주장·출처를 프로젝트별로 탐색할 수 없었다.
- 관심사를 나중에 다른 프로젝트로 옮기면 생성 당시의 불변 `project_id` 스냅샷을 가진 과거 캠페인까지 backfill과 검증이 충돌로 거부했다.
- 수동 `campaign-add` 경로는 관심사의 `research_brief`를 캠페인에 복사하지 않았다.
- `PRJ-` 접두사만 검사한 프로젝트 ID가 경로 구분자와 `..`를 포함할 수 있어 프로젝트 렌더가 `wiki/projects/` 밖에 쓰는 입력을 거부하지 않았다.
- 프로젝트 파생 페이지의 OKF `timestamp`가 프로젝트 설정 시각에 고정돼 더 최신 캠페인·실행이 표시돼도 의미 변경 시각을 반영하지 않았다.

## Red 명령과 결과

`python3 -m unittest -v tests.test_wiki.IntegratedGateTests.test_interest_seed_enforces_one_global_slot_per_local_day tests.test_wiki.IntegratedGateTests.test_two_daily_interests_rotate_and_failed_question_is_retried tests.test_wiki.IntegratedGateTests.test_interest_seed_copies_research_brief_into_campaign tests.test_wiki.IntegratedGateTests.test_interest_seed_rejects_missing_or_unknown_project tests.test_wiki.IntegratedGateTests.test_project_views_group_campaigns_runs_and_shared_evidence_without_duplication tests.test_living_wiki_steward_skill.LivingWikiStewardSkillContractTests.test_prd_has_all_acceptance_ids_and_entrypoints_link_it tests.test_living_wiki_steward_skill.LivingWikiStewardSkillContractTests.test_prd_v1_5_contracts_hygiene_time_github_daily_research_and_projects`

결과는 7개 중 공정 순환과 명세 계약 3개가 통과하고, 전역 상한·프로젝트 복사·잘못된 프로젝트 거부·프로젝트 뷰 4개가 예상대로 실패했다. 기존 ID 정렬을 최신 생성 순서로 오인하던 fixture는 `cycle_key`로 고쳐 제품 계약을 약화하지 않았다.

독립 최종 검토에서 관심사 이동 회귀를 추가로 고정했다. `python3 -m unittest tests.test_wiki.IntegratedGateTests.test_campaign_project_snapshot_survives_later_interest_move -v`는 과거 캠페인 `PRJ-WIKI-HARNESS`와 현재 관심사 `PRJ-AGENT-TRAINING-PAPER`가 다르다는 이유로 예상대로 오류가 났다.

수동 진입점과 경로 경계 회귀도 고정했다. `test_manual_campaign_add_copies_research_brief_into_campaign`은 `research_brief` 부재로 오류가 났고, `test_project_id_path_traversal_is_rejected_before_render`는 `PRJ-X/../../../outside`를 거부하지 않아 실패했다. 후자는 임시 fixture 안에서만 재현했으며 외부 실행 권한은 사용하지 않았다.

프로젝트 페이지 시간 의미 회귀는 `test_project_views_group_campaigns_runs_and_shared_evidence_without_duplication`에서 고정했다. 2026-07-16 캠페인이 표시돼도 frontmatter가 2026-07-15로 남아 예상대로 실패했다.

## Green 기준

- 두 프로젝트 레지스트리와 관심사 참조를 fail closed로 검증한다.
- 캠페인은 관심사의 `project_id`와 `research_brief`를 깊은 복사로 보존한다.
- 하루 상한은 생성 행위자와 무관하게 두 프로젝트 전체에 적용한다.
- 프로젝트별 파생 페이지는 캠페인과 실행을 분리해 보여주고 같은 canonical claim/source 링크를 공유한다.
- 기존 캠페인은 ID와 증거 관계를 바꾸지 않고 `Wiki 하네스 연구`로 한 번 분류한다.
- 관심사가 이동해도 이미 분류된 캠페인의 등록된 프로젝트 스냅샷은 바꾸거나 거부하지 않는다.
- 수동·예약 캠페인 생성 경로 모두 같은 불변 `research_brief`를 복사한다.
- 프로젝트 ID는 파일명으로 안전한 canonical 형식만 허용하고 경로 구분자·점 세그먼트를 렌더 전에 거부한다.
- 프로젝트 페이지 `timestamp`는 단순 재렌더 시각이 아니라 표시된 프로젝트·관심사·캠페인·실행의 최신 의미 변경 시각을 투영한다.
